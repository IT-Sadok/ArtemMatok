name: CI/CD Pipeline for BookingWebApi

on:
  push:
    branches:
      - main
      - feature/ci-cd
  pull_request:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 8.0

      - name: Restore dependencies
        run: dotnet restore BookingProject/BookingWebAPI/BookingWebApi/BookingWebApi.csproj

      - name: Build project
        run: dotnet build BookingProject/BookingWebAPI/BookingWebApi/BookingWebApi.csproj --no-restore --configuration Release

      - name: Publish project
        run: dotnet publish BookingProject/BookingWebAPI/BookingWebApi/BookingWebApi.csproj --configuration Release --self-contained false --framework net8.0 --output ./publish

      - name: Verify runtimeconfig
        run: |
          if [ ! -f "./publish/BookingWebApi.runtimeconfig.json" ]; then
            echo "Runtimeconfig.json file not found!"
            exit 1
          fi

      - name: List publish directory
        run: find ./publish -type f

      - name: Zip published files
        run: zip -r BookingWebApi.zip ./publish

      - name: Create S3 bucket
        run: |
          aws s3api create-bucket --bucket booking-webapi-deployments --region us-east-2 --create-bucket-configuration LocationConstraint=us-east-2 || echo "Bucket already exists"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Upload to S3
        run: aws s3 cp BookingWebApi.zip s3://booking-webapi-deployments/BookingWebApi.zip
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Deploy to AWS Elastic Beanstalk
        uses: einaregilsson/beanstalk-deploy@v20
        with:
          aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          application_name: "booking-webapi"
          environment_name: "Development"
          region: "us-east-2"
          version_label: ${{ github.sha }}
          deployment_package: "BookingWebApi.zip"
          existing_bucket_name: "booking-webapi-deployments"
